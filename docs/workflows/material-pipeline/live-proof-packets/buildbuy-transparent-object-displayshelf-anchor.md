# Build/Buy Transparent Object DisplayShelf Anchor

This packet records the strongest current survey-backed transparent-object anchor inside the `EP10` transparent-decor cluster.

Question:

- what exact local identity and companion-bundle evidence already makes `displayShelf` the safest first transparent-object reopen target?

Related docs:

- [Build/Buy Transparent-Decor Route](buildbuy-transparent-decor-route.md)
- [Build/Buy Transparent Object Target Priority](buildbuy-transparent-object-target-priority.md)
- [Build/Buy Transparent Object Reopen Checklist](buildbuy-transparent-object-reopen-checklist.md)
- [P1 Live-Proof Queue](../p1-live-proof-queue.md)

## Scope status (`v0.1`)

```text
Build/Buy Transparent Object DisplayShelf Anchor
├─ Survey-backed identity capture ~ 96%
├─ Companion-bundle anchor strength ~ 97%
├─ Restart-safe first-target wording ~ 96%
└─ Stable live-fixture closure ~ 21%
```

## Current local identity

Current `tmp/probe_ep10_buildbuy_identity_survey_full.json` is already strong enough to record:

- `ObjectDefinitionTgi = C0DB5AE7:00000000:000000000003E787`
- `ObjectCatalogTgi = 319E4F1D:00000000:000000000003E787`
- `ObjectDefinitionInternalName = shelfFloor2x1_EP10TEAdisplayShelf_set1`

Current `tmp/probe_ep10_buildbuy_candidate_resolution_full.json` is already strong enough to record:

- `Model -> 01661233:00000000:E779C31F25406B73` via `instance-swap32`, `SourceCount = 10`
- `Rig -> 8EAF13DE:00000000:E779C31F25406B73` via `instance-swap32`, `SourceCount = 10`
- `Slot -> D3044521:00000000:E779C31F25406B73` via `instance-swap32`, `SourceCount = 10`
- `Footprint -> D382BF57:00000000:E779C31F25406B73` via `instance-swap32`, `SourceCount = 10`

Current `tmp/probe_all_buildbuy.txt` is already strong enough to record:

- `C:\GAMES\The Sims 4\EP10\ClientFullBuild0.package|01661233:00000000:E779C31F25406B73`

## Safe reading

This anchor currently proves:

- `displayShelf` is the strongest current transparent-object survey anchor
- the route does not depend only on vague transparent naming
- the current first target really does preserve the full companion bundle:
  - `Model`
  - `Rig`
  - `Slot`
  - `Footprint`

This anchor does not yet prove:

- a stable reopened `Build/Buy` asset
- a winning transparent-family branch
- object-glass, threshold/cutout, `AlphaBlended`, or `SimGlass`

## Why this matters

Without this packet, the current first target would still read mostly as:

- “use `displayShelf` first because it seems strongest”

The current evidence is already better than that:

- `displayShelf` is now a named, survey-backed, full-companion anchor with restart-safe identity

## Best next use of this packet

Use `displayShelf` as the first true reopen attempt for the transparent-object branch.

If that reopen still does not materialize as a stable fixture, the next target remains:

- `shopDisplayTileable`

not because `displayShelf` was arbitrary, but because the current anchor packet has already been exhausted honestly.
