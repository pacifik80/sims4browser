# Refraction 0389 Identity Gap

This packet records the current identity ceiling on the `0389...` clean route: it is coverage-backed, but not yet named to the same level as `lilyPad`.

Question:

- what is still missing before `0389A352F5EDFD45` can be treated as more than a coverage-backed clean route?

Related docs:

- [Material Pipeline Deep Dives](../README.md)
- [Refraction 0389 Clean-Route Baseline](refraction-0389-clean-route-baseline.md)
- [RefractionMap Live Proof](refractionmap-live-proof.md)

## Scope status (`v0.1`)

```text
Refraction 0389 Identity Gap
├─ Coverage-versus-fixture boundary ~ 96%
├─ Named-identity gap capture ~ 97%
├─ Anti-overread discipline ~ 95%
└─ Exact refraction-slot closure ~ 22%
```

## Current local limit

Current workspace support for `0389A352F5EDFD45` is still only:

- `tmp/probe_sample_medium_coverage.txt`
- `EP11\ClientFullBuild0.package`
- `Build/Buy Model 0389A352F5EDFD45`
- `Found: True`
- `Scene Success: True`
- `WorldToDepthMapSpaceMatrix=1`
- `ProjectiveMaterialDecodeStrategy=1`

Current workspace does not yet add:

- a named object row comparable to `sculptFountainSurface3x3_EP10GENlilyPad`
- an `OBJD` / `COBJ`-backed identity chain
- a direct object/material companion seam comparable to the named `lilyPad` bridge

## Safe reading

`0389...` is currently strong enough to be:

- the next clean route baseline

It is not yet strong enough to be:

- a named refraction fixture
- a replacement for the current `lilyPad` named bridge/root floor

## Why this matters

Without this packet, the route could drift from:

- coverage-backed clean route

into:

- nearly equivalent named fixture

The current evidence does not support that promotion yet.

## What remains open

- whether a narrower package/identity sweep can name `0389...` the way `lilyPad` was named
- whether a stronger material seam can surface before that naming step
