# Refraction Adjacent-Helper Boundary

This packet keeps adjacent projective/helper evidence around the named `lilyPad` fixture from being overread as direct `RefractionMap` family closure.

Question:

- how should the current projective-helper neighborhood around the `lilyPad` bridge root be recorded without pretending that helper survival already proves direct refraction-family slot closure?

Related docs:

- [Material Pipeline Deep Dives](../README.md)
- [Projection, Reveal, And Generated-Light Boundary](../projection-reveal-generated-light-boundary.md)
- [Refraction Evidence Ledger](../refraction-evidence-ledger.md)
- [Refraction Bridge Fixture Boundary](../refraction-bridge-fixture-boundary.md)
- [RefractionMap Live Proof](refractionmap-live-proof.md)

## Scope status (`v0.1`)

```text
Refraction Adjacent-Helper Boundary
├─ Projective-helper preservation ~ 94%
├─ Helper-versus-family closure boundary ~ 96%
├─ Mixed-root control boundary ~ 92%
└─ Exact refraction-slot closure ~ 21%
```

## Safe boundary

The current workspace already keeps a real adjacent-helper neighborhood around the refraction row:

- `gPosToUVDest`
- `WorldToDepthMapSpaceMatrix`
- `FresnelOffset`

Safe reading:

- these names are useful boundary evidence for the projective neighborhood
- they are not direct proof that `RefractionMap` itself is solved at the slot layer

## Why this matters

The strongest named bridge root currently survives through a projective/material-adjacent packet.

That means:

- the neighborhood is real
- helper adjacency matters

It does not mean:

- helper adjacency equals direct `RefractionMap` closure

## Mixed-root control

The current weaker control root `0124E3B8AC7BEE62` already shows why this boundary matters:

- mixed `WorldToDepthMapSpaceMatrix`
- one `FresnelOffset` LOD
- weaker fallback diffuse path

Safe reading:

- helper-heavy or mixed projective roots are valid comparison controls
- they should not be used to silently widen the refraction family into one generic projective bucket

## Unsafe shortcut

Do not record:

- “projective helper present, so refraction solved”
- “`WorldToDepthMapSpaceMatrix` means direct refraction semantics”
- “`FresnelOffset` means this is already the same thing as the refraction-family slot layer”

## What remains open

- which helper names survive directly on the named `lilyPad` seam versus only in adjacent comparison roots
- whether the named fixture reaches direct family-local refraction evidence beyond helper adjacency
